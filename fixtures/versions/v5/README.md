# Immutable v5 fixture corpus

This directory preserves the complete 12-case report/Semantic IR corpus emitted
under `codeskeptic.semantic-verification/v5` before A6.5 introduced modular-call
frame summaries and normalized modified-location metadata.

`SHA256SUMS` covers the archived manifest, all source cases, and all expected
artifacts. These files are historical wire evidence and must not be rewritten.
