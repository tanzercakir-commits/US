# Immutable v4 fixture corpus

This directory preserves the complete 11-case report/Semantic IR corpus emitted
under `codeskeptic.semantic-verification/v4` before A6.4 introduced
proof-bearing reference-binding metadata.

`SHA256SUMS` covers the archived manifest, all source cases, and all expected
artifacts. These files are historical wire evidence and must not be rewritten.
