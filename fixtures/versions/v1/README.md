# Archived v1 Fixture Corpus

This directory preserves the exact report, Semantic IR, source, and manifest
bytes produced immediately before the A6.8 schema-major migration.

The v1 corpus uses `codeskeptic.semantic-verification/v1`, the implicit `int`
type spelling, JSON-number integer constants, and JSON-number integer
counterexample bindings. It is immutable compatibility evidence and is never a
regeneration target. Current fixtures live in `fixtures/expected`.
