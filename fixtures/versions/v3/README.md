# Semantic verification fixture archive v3

This immutable archive preserves the complete fixture corpus immediately before
A6.3 introduced owned value records and advanced the report/Semantic IR schema
to `codeskeptic.semantic-verification/v4`.

`SHA256SUMS` covers `manifest.json`, every source case, and every expected IR and
report artifact. Paths are relative to this directory and use `/` separators.
The archive is migration evidence only; current fixture regeneration must never
write here.
